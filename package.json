{
  "name": "vertex-ops-pose-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.mjs",
    "check:syntax": "node --check app.js && node --check server.js && node --check sw.js && node --check imap-worker.js && node --check lead-fingerprint.mjs && node --check lib/work-report-pdf.js && node --check scripts/restore-snapshot.mjs",
    "check:shell-version": "node scripts/verify-shell-version.mjs",
    "test": "node --test",
    "check": "npm run check:syntax && npm run check:shell-version && npm test",
    "bump": "node scripts/bump-shell-version.mjs",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.883.0",
    "imapflow": "^1.0.165",
    "mailparser": "^3.7.0",
    "pdfkit": "^0.18.0",
    "pg": "^8.16.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
